apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skillsmanager.fullname" . }}-test-connection"
  labels:
    {{- include "skillsmanager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skillsmanager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
